# 평가
deep-translator>=1.9.0 # Google 번역 (comet_eval.py)
unbabel-comet>=2.2.0   # COMET MT 평가 지표 (comet_eval.py, autoseg 기본 품질 백엔드)

# GPT 분절
openai>=1.0.0          # gpt_seg.py

# 프롬프트 자동 생성 루프 (autoseg/)
httpx>=0.27.0          # Letsur AI Gateway 호출 + Google 번역
langcodes[data]>=3.5   # 언어 이름 -> 코드 (pipeline.to_lang_code). 표를 코드에 안 박으려고 쓴다
setuptools<81          # torchmetrics 0.10.x 가 pkg_resources 를 import 한다 (81+ 에서 제거됨)
#
# 지표 백엔드 두 개 모두 unbabel-comet + GPU 가 필요하다.
#   adequacy    (y축 주지표, 참조 없음)  Unbabel/wmt22-cometkiwi-da
#   consistency (보고용, 참조 기반)      Unbabel/wmt22-comet-da
#
# **CometKiwi 는 HF 게이트 모델이다.** huggingface.co 에서 라이선스에 동의하고
#   hf auth login        # huggingface_hub 1.x. 구버전은 huggingface-cli login
# 을 먼저 해야 adequacy 백엔드가 뜬다. 안 하면 루프가 시작 즉시 실패한다.
#
# consistency 는 GPU 없이 --consistency-backend embed / chrf 로 낮출 수 있으나
# 타당도 검사에서 comet 보다 나쁘다 (validity_check.py). adequacy 에는 대체재가 없다.
#
# torch 는 환경에 맞는 빌드를 따로 설치할 것:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
