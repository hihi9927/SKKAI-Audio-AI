"""참조 기반 평가와 논문 그림 — 루프가 낸 프롬프트를 바깥 자로 다시 잰다.

`bleu_eval` 이 조건별 번역·BLEU/chrF2·쌍체 부트스트랩·실측 LAAL 을 내고, `comet_eval` 은
그 번역을 재사용해 COMET 을 얹는다(재번역 없음). `comet_x2en` 은 X→en 트랙 전용이고,
`eval_prompt` 는 임의의 프롬프트 하나를 루프와 같은 지표로 잰다. `plot_*` 는 그 산출로
품질–지연 곡선을 그린다.

**`plot_*` 는 import 하면 안 된다** — 모듈 최상위에서 그림을 저장하므로 읽는 것만으로
git 에 추적되는 산출물이 덮인다. 각 파일의 가드가 막는다.
"""
