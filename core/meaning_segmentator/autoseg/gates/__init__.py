"""루프를 돌리기 **전에** 통과해야 하는 타당도 관문 — 루프 밖, 1회성.

측정 도구 자체가 틀렸는지 본다. 지표가 틀리면 숫자로 드러나지만 **조향은 조용히
발산한다** — v1 에서 embed 백엔드가 부정 뒤집힘에 최고점을 줘 5회 런이 통째로 무효가 된
것이 그 사례다. `validity_check` 는 품질 백엔드를, `judge_check` 는 판정자(모델+프롬프트)를,
`adequacy_check` 는 조각 입력에서의 참조 없는 QE 를, `noise_floor` 는 NLI 가 무해한
미완성에 주는 기본 모순 확률을 잰다. 케이스 JSON 은 사람이 쓴 고정 입력이다.
"""
