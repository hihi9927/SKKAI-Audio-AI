# 유형별 의미 축 여러 개 — 다축 구성

인코더 `intfloat/multilingual-e5-large-instruct` · MNLI 40000쌍 · run04 경계 1003개 · **라벨 0건**(축은 전부 규칙 생성)

`semantic_axis` 에서 부정 축 하나로 관문 3/6 이었다. 관문의 실패 유형이 다섯인데 축 하나가 하나를 덮기 때문이었으므로, 유형마다 축을 만들어 결합한다.

## ① 최소쌍 수율

| 축 | 규칙 | 최소쌍 |
|---|---|---|
| `negation` | is → is not | 3000 |
| `role` | 고유명사·대명사 교환 | 3000 |
| `modality` | will → might | 3000 |
| `quantifier` | all → some | 3000 |
| `tense` | is → was | 3000 |
| `content_swap` | 내용어 두 개 교환 (**대조군**) | 3000 |

## ② 축들이 서로 다른 방향인가 (Gram 행렬)

**전부 같은 쪽을 가리키면 축이 다섯 개가 아니라 하나를 다섯 번 만든 것이다.** 무작위 방향 두 개의 코사인 기대값 ≈ 0.031.

| | content_swap | modality | negation | quantifier | role | tense |
|---|---|---|---|---|---|---|
| **content_swap** | 1.000 | 0.177 | -0.006 | 0.389 | 0.608 | 0.075 |
| **modality** | 0.177 | 1.000 | 0.138 | 0.362 | 0.111 | 0.165 |
| **negation** | -0.006 | 0.138 | 1.000 | -0.128 | 0.016 | 0.019 |
| **quantifier** | 0.389 | 0.362 | -0.128 | 1.000 | 0.273 | 0.181 |
| **role** | 0.608 | 0.111 | 0.016 | 0.273 | 1.000 | 0.104 |
| **tense** | 0.075 | 0.165 | 0.019 | 0.181 | 0.104 | 1.000 |

MNLI 모순 축과의 정렬:

| 축 | 모순 축과 코사인 |
|---|---|
| `content_swap` | -0.1653 |
| `modality` | -0.2044 |
| `negation` | 0.5754 |
| `quantifier` | -0.643 |
| `role` | -0.071 |
| `tense` | -0.0147 |

## ③ MNLI dev 모순 AUC — 축별과 결합

| 읽기 | 모순 AUC |
|---|---|
| axis:content_swap | **0.4844** |
| axis:modality | **0.516** |
| axis:negation | **0.7565** |
| axis:quantifier | **0.6444** |
| axis:role | **0.4487** |
| axis:tense | **0.4774** |
| cos (거리 기준선) | **0.7217** |
| max(negation+role+modality+quantifier+tense) | **0.7294** |
| l2(모든 이름 축) | **0.7257** |
| max(축) + cos | **0.7964** |

## ④ 관문 (`premature_cases.json`)

| 채점기 | 변이 | 위반/케이스 | mean(prem) | mean(safe) | 격차 | 판정 |
|---|---|---|---|---|---|---|
| `multi:max` | raw | 2/6 | 0.2636 | 0.2118 | 0.0518 | **탈락** |
| `multi:max` | floor | 4/6 | 0.1326 | 0.076 | 0.0566 | **탈락** |
| `multi:max` | z | 2/6 | 1.5813 | 0.721 | 0.8603 | **탈락** |
| `multi:l2` | raw | 3/6 | 0.303 | 0.2581 | 0.0449 | **탈락** |
| `multi:l2` | floor | 4/6 | 0.131 | 0.0729 | 0.0581 | **탈락** |
| `multi:l2` | z | 3/6 | 0.9997 | 0.4031 | 0.5965 | **탈락** |
| `multi:max+cos` | raw | 2/6 | 0.3857 | 0.3421 | 0.0437 | **탈락** |
| `multi:max+cos` | floor | 4/6 | 0.1064 | 0.0447 | 0.0617 | **탈락** |
| `multi:max+cos` | z | 2/6 | 0.7462 | 0.0113 | 0.7349 | **탈락** |

### 유형이 맞는 축이 실제로 켜지는가

케이스마다 `premature 성분 − safe 성분`. **양수 = 그 축이 조기 방출 쪽에서 더 켜졌다.** 유형과 축이 대응한다면 대각선이 밝아야 한다.

| 케이스 (유형) | content_swap | modality | negation | quantifier | role | tense |
|---|---|---|---|---|---|---|
| ko-en-p01 (premature_negation) | — | +0.004 | **-0.057** | -0.056 | +0.031 | -0.004 |
| ko-en-p02 (premature_role) | — | -0.042 | +0.013 | +0.021 | **-0.017** | -0.158 |
| ko-en-p03 (premature_scope) | — | +0.006 | -0.036 | **+0.016** | -0.006 | +0.098 |
| ko-en-p04 (premature_head) | — | +0.114 | -0.057 | -0.066 | -0.059 | **-0.062** |
| ko-en-p06 (premature_modal) | — | **+0.167** | +0.007 | +0.146 | -0.043 | -0.007 |
| ja-ko-p05 (premature_negation) | — | +0.043 | **+0.101** | -0.035 | +0.000 | +0.004 |

굵은 칸이 유형에 대응하는 축이다.

## ⑤ 잡음 바닥과 실데이터

| 채점기 | 바닥 mean | 바닥 sd | 1-2 | 3-4 | 5-6 | 7-9 | 10-14 | 15+ |
|---|---|---|---|---|---|---|---|---|
| `multi:max` | 0.1644 | 0.0683 | 0.1692 | 0.1608 | 0.1635 | 0.1654 | 0.168 | 0.1573 |
| `multi:l2` | 0.2214 | 0.0855 | 0.2264 | 0.22 | 0.2228 | 0.2236 | 0.2247 | 0.2094 |
| `multi:max+cos` | 0.2815 | 0.1035 | 0.3986 | 0.3515 | 0.3086 | 0.2685 | 0.2321 | 0.2013 |

| 채점기 | 변이 | 현행 NLI 와 Spearman | topk 겹침 |
|---|---|---|---|
| `multi:max` | raw | 0.0424 | 0.15 |
| `multi:max` | floor | 0.0821 | 0.1 |
| `multi:max` | z | 0.0355 | 0.1 |
| `multi:l2` | raw | 0.0147 | 0.05 |
| `multi:l2` | floor | 0.0411 | 0.05 |
| `multi:l2` | z | 0.0115 | 0.1 |
| `multi:max+cos` | raw | 0.3133 | 0.3 |
| `multi:max+cos` | floor | 0.0923 | 0.1 |
| `multi:max+cos` | z | 0.0986 | 0.1 |

## 판정

**축들은 서로 구별된다.** Gram 비대각 성분의 평균 절대값 0.183, 최대 0.608 — 무작위 기대(0.031)보다는 크지만 1 과는 멀다. 다섯 축은 겹치되 같지 않다.

**단 `role` 은 예외다 — 대조군(`content_swap`)과 코사인 0.608.** 참여자 교환은 **토큰 집합이 그대로**이고 배열만 바뀐다. 풀링된 임베딩에서 그것은 '단어가 섞였다' 와 구별되지 않는다. 즉 `role` 축은 참여자 의미를 잡은 것이 아니라 어순 교란을 잡은 것이다.

### 잘 되는 축과 안 되는 축이 갈린다

| 축 | MNLI 모순 AUC | 성질 |
|---|---|---|
| axis:content_swap | 0.4844 | 대조군 |
| axis:modality | 0.516 | 배열·문법 자질만 바뀐다 |
| axis:negation | 0.7565 | **토큰이 바뀐다** |
| axis:quantifier | 0.6444 | **토큰이 바뀐다** |
| axis:role | 0.4487 | 배열·문법 자질만 바뀐다 |
| axis:tense | 0.4774 | 배열·문법 자질만 바뀐다 |

**어휘가 실제로 치환되는 유형**(부정 `not` 삽입, 양화 `all→some`)만 신호를 낸다. 대조군은 0.4844 로 우연 수준이라, '이름' 이 하는 일이 있다는 것은 확인된다.

> **주의 — MNLI 모순 AUC 는 `modality`·`tense` 에는 맞지 않는 자다.** 서법·시제 변화는 MNLI 에서 대개 `neutral` 로 라벨링되지 모순이 아니다. 우리 문제에서는 그것도 실패이므로, 이 두 축의 평가는 아래 케이스별 표로 봐야 한다 — 실제로 `modality` 축은 `ko-en-p06`(서법 확정)에서 가장 크게 켜졌다(+0.167).

### 결합이 오히려 해롭다

축 다섯 개의 max 가 0.7294 인데 **부정 축 단독이 0.7565** 다 (-0.027). 잡음 축(우연 수준의 셋)이 max 를 자주 가져가기 때문이다. **max 결합은 최악의 축에 지배된다** — 양방향 NLI 의 `min` 이 두 방향 다 의미가 있어서 성립하는 것과 대조된다. 여기서는 축마다 신뢰도가 달라 같은 발상이 성립하지 않는다.

거리(코사인 0.7217)를 더한 `max(축)+cos` 가 0.7964 로 가장 높은데, 이는 축의 기여가 아니라 **거리 축이 여전히 주력**이라는 뜻이다.

### 관문과 유형 대응

**관문은 여전히 탈락이다** (최선 2/6). 단축 3/6 에서 2/6 으로 개선은 됐다.

유형에 대응하는 축이 실제로 켜진 케이스는 **6건 중 3건**이다. `ko-en-p01`(부정)에서 부정 축이 **음수**로 나오는 것이 대표적 실패인데, 안전 변이(`As for that,`)가 내용이 거의 없는 조각이라 차이벡터의 방향이 사실상 임의적이기 때문이다 — `semantic_axis` 에서 확인한 것과 같은 문제이고, 축을 늘려도 해결되지 않는다.

### 결론 — 여기서 멈추는 것이 맞다

1. **축 개수를 늘리는 방향은 수익이 체감한다.** 이득은 어휘 치환형 두 축에 몰려 있고, 나머지를 더하면 max 결합에서는 오히려 깎인다.
2. **구조적 실패는 방향으로 표현되지 않는다.** 참여자 교환은 토큰 집합이 보존되므로 풀링된 벡터에서 어순 교란과 구별되지 않는다(`role` vs `content_swap` 코사인 0.608). 이것이 교차 인코더와의 격차의 실체다 — 누가 누구에게 했는지는 두 문장의 **토큰이 서로를 봐야** 판정된다.
3. **유형 목록은 임의 상수다.** 목록에 없는 실패는 구조적으로 못 잡는다. 이 레포가 임의 상수를 계속 줄여온 방향과 반대이고, 지금 이득의 크기가 그 비용을 정당화하지 못한다.

축 탐색으로 얻은 것은 대체재가 아니라 **진단**이다: 임베딩이 잡는 의미 변화는 어휘 치환형이고, 못 잡는 것은 구조형이다.