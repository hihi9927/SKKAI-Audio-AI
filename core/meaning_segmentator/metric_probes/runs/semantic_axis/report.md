# 임베딩에서 NLI 와 같은 역할을 하는 축 찾기

인코더 `intfloat/multilingual-e5-large-instruct` · 규칙 부정 최소쌍 4000개 · MNLI 40000쌍 · run04 경계 1003개

`embed_geometry` 결론은 '차이벡터가 등방적이라 **분산 기준으로는** 읽을 축이 안 나온다' 였다. 여기서는 축을 **대조쌍**으로 만든다 — 그 속성만 다른 문장 쌍의 평균 차이벡터.

## ① 축에 이름이 붙는가 — 축끼리의 정렬

규칙으로 만든 **부정 방향**과 데이터로 배운 **모순 방향**이 같은 쪽을 가리키는가. 높으면 그 축은 회귀 계수가 아니라 **실재하는 의미 방향**이다.

| 축 쌍 | 코사인 |
|---|---|
| neg_rule vs mnli_mean | **0.5865** |
| neg_rule vs mnli_lda | **0.0352** |
| mnli_mean vs mnli_lda | **0.1184** |

참고: 무작위 방향 두 개의 코사인 기대값 ≈ 0.0312 (1024차원).

## ② 1차원 축이 NLI 를 얼마나 대신하는가 (MNLI dev)

같은 dev 집합에서 **모순 vs 나머지** AUC.

| 읽기 | 차원 | 모순 AUC |
|---|---|---|
| cos (거리, 1차원) | 1 | **0.7217** |
| axis:neg_rule (라벨 없음) | 1 | **0.7839** |
| axis:mnli_mean (클래스 평균) | 1 | **0.7976** |
| axis:mnli_lda (Fisher) | 1 | **0.7363** |
| axis:mnli_lda 부호 있음 | 1 | **0.7363** |
| probe (전체 지도, 참고) | 2048 | **0.8701** |

`cos`·`probe`·교차 인코더 수치는 `embed_probe.py` 와 같은 자로 잰 것이다.

## ③ 관문 (`premature_cases.json`)

통과 조건은 `judge_check.check_nli` 와 동일: `min(premature) > max(safe)`.

| 채점기 | 변이 | 위반/케이스 | mean(prem) | mean(safe) | 격차 | 판정 |
|---|---|---|---|---|---|---|
| `axis:neg_rule` | raw | 4/6 | 0.0732 | 0.0747 | -0.0015 | **탈락** |
| `axis:neg_rule` | floor | 4/6 | 0.0353 | 0.0289 | 0.0064 | **탈락** |
| `axis:neg_rule` | z | 3/6 | 0.4677 | 0.28 | 0.1877 | **탈락** |
| `axis:mnli_mean` | raw | 3/6 | 0.0485 | 0.0546 | -0.0061 | **탈락** |
| `axis:mnli_mean` | floor | 5/6 | 0.0124 | 0.0202 | -0.0078 | **탈락** |
| `axis:mnli_mean` | z | 2/6 | -0.2087 | -0.2096 | 0.0009 | **탈락** |
| `axis:mnli_lda` | raw | 3/6 | 0.008 | 0.008 | -0.0 | **탈락** |
| `axis:mnli_lda` | floor | 4/6 | 0.0026 | 0.0025 | 0.0001 | **탈락** |
| `axis:mnli_lda` | z | 2/6 | 0.1073 | -0.0677 | 0.175 | **탈락** |
| `axis:neg_rule/norm` | raw | 3/6 | 0.1833 | 0.1651 | 0.0182 | **탈락** |
| `axis:neg_rule/norm` | floor | 4/6 | 0.1155 | 0.0866 | 0.0289 | **탈락** |
| `axis:neg_rule/norm` | z | 3/6 | 1.2352 | 0.822 | 0.4132 | **탈락** |
| `axis:mnli_mean/norm` | raw | 3/6 | 0.1152 | 0.112 | 0.0032 | **탈락** |
| `axis:mnli_mean/norm` | floor | 5/6 | 0.0519 | 0.0559 | -0.004 | **탈락** |
| `axis:mnli_mean/norm` | z | 3/6 | 0.138 | 0.0709 | 0.0671 | **탈락** |
| `axis:mean/norm+cos` | raw | 4/6 | 0.2374 | 0.2423 | -0.0049 | **탈락** |
| `axis:mean/norm+cos` | floor | 4/6 | 0.0258 | 0.0362 | -0.0104 | **탈락** |
| `axis:mean/norm+cos` | z | 3/6 | -0.2485 | -0.4211 | 0.1726 | **탈락** |

## ④ 잡음 바닥과 실데이터

| 채점기 | 바닥 mean | 바닥 sd | 1-2 | 3-4 | 5-6 | 7-9 | 10-14 | 15+ |
|---|---|---|---|---|---|---|---|---|
| `axis:neg_rule` | 0.042 | 0.0442 | 0.0734 | 0.0544 | 0.0474 | 0.0394 | 0.0316 | 0.0217 |
| `axis:mnli_mean` | 0.0465 | 0.0476 | 0.0724 | 0.064 | 0.0567 | 0.0454 | 0.0342 | 0.0239 |
| `axis:mnli_lda` | 0.0058 | 0.0056 | 0.0098 | 0.0088 | 0.0069 | 0.0052 | 0.0036 | 0.0032 |
| `axis:neg_rule/norm` | 0.0886 | 0.0832 | 0.1081 | 0.0881 | 0.0882 | 0.0862 | 0.0885 | 0.0761 |
| `axis:mnli_mean/norm` | 0.098 | 0.0886 | 0.1069 | 0.1034 | 0.1048 | 0.0988 | 0.0961 | 0.0832 |
| `axis:mean/norm+cos` | 0.2151 | 0.1222 | 0.3363 | 0.2941 | 0.2498 | 0.2019 | 0.1602 | 0.1272 |

| 채점기 | 변이 | 현행 NLI 와 Spearman | topk 겹침 |
|---|---|---|---|
| `axis:neg_rule` | raw | 0.1711 | 0.3 |
| `axis:neg_rule` | floor | 0.1101 | 0.3 |
| `axis:neg_rule` | z | 0.0627 | 0.1 |
| `axis:mnli_mean` | raw | 0.1158 | 0.0 |
| `axis:mnli_mean` | floor | 0.0508 | 0.05 |
| `axis:mnli_mean` | z | 0.0212 | 0.0 |
| `axis:mnli_lda` | raw | 0.0764 | 0.15 |
| `axis:mnli_lda` | floor | -0.0153 | 0.1 |
| `axis:mnli_lda` | z | -0.0487 | 0.05 |
| `axis:neg_rule/norm` | raw | 0.103 | 0.3 |
| `axis:neg_rule/norm` | floor | 0.0918 | 0.3 |
| `axis:neg_rule/norm` | z | 0.0625 | 0.2 |
| `axis:mnli_mean/norm` | raw | 0.0426 | 0.0 |
| `axis:mnli_mean/norm` | floor | 0.0334 | 0.0 |
| `axis:mnli_mean/norm` | z | 0.0256 | 0.0 |
| `axis:mean/norm+cos` | raw | 0.2776 | 0.0 |
| `axis:mean/norm+cos` | floor | 0.0762 | 0.1 |
| `axis:mean/norm+cos` | z | 0.0835 | 0.05 |

## 판정

**축에는 이름이 붙는다.** 규칙으로 만든 부정 방향과 MNLI 에서 배운 모순 방향의 코사인이 **0.5865** 로, 무작위 기대값(≈0.031)을 크게 넘는다. 즉 '극성' 은 임베딩 공간에 실재하는 방향이고, 라벨 없이 규칙만으로도 찾아낼 수 있다.

**그런데 관문은 통과하지 못했다** (최선 2/6). 축을 **찾는 것**과 그 축으로 우리 fixture 를 **가르는 것**은 별개였다.

관문의 실패 유형은 다섯 가지다 — 극성 뒤집힘(`premature_negation`), 참여자 뒤바뀜(`premature_role`), 범위 변화(`premature_scope`), 핵어 확정(`premature_head`), 서법 확정(`premature_modal`). **극성 축 하나가 덮는 것은 그중 하나다.** 게다가 케이스별 통과/실패가 유형과 깔끔히 대응하지도 않는다 — `ko-en-p01` 은 극성 케이스인데도 실패하는데, 그쪽 안전 변이(`As for that,`)가 내용이 거의 없는 조각이라 차이벡터의 방향이 사실상 임의적이기 때문이다. **내용이 희박한 조각에서는 방향 자체가 정의되지 않는다.**

정규화는 도움이 됐다 (`neg_rule` 4/6 → `neg_rule/norm` 3/6). 투영을 `‖d‖` 로 나눠 '변화 중 이 축의 비율' 로 바꾸면 크기와 종류가 분리된다 — 안 하면 짧은 조각이 큰 `‖d‖` 때문에 극성 성분까지 크게 받는다. 그래도 관문을 넘기지는 못한다.

**다음 수는 축을 여러 개 만드는 것이다.** 참여자 뒤바뀜은 문장의 고유명사 두 개를 기계적으로 맞바꿔 최소쌍을 만들 수 있고(부정 규칙과 같은 방식, 라벨 0), 서법은 조동사 치환으로 만들 수 있다. 유형별 축을 세우고 그 성분들의 최대값을 쓰면 다섯 유형을 각각 겨냥할 수 있다. 다만 그때도 **유형 목록이 곧 임의 상수**가 된다는 점은 감안해야 한다 — 목록에 없는 실패는 못 잡는다. NLI 가 하나의 모델로 그 전부를 덮는 이유가 그것이다.