"""지표 백엔드 탐침 — `contradiction`(NLI) 자리를 무엇으로 대신할 수 있는지 재는 실험들.

`autoseg/` 는 **루프가 실제로 쓰는 것**만 둔다. 여기 있는 것은 어느 것도 루프 경로에
들어가지 않으며, 기존 런(`../runs/`)을 읽어 재활용할 뿐이다. 결론과 근거는
[../NLI_ALTERNATIVES.md](../NLI_ALTERNATIVES.md).
"""
