# ============================================================
# requirements.txt
# 환경: Ubuntu 22.04 / 24.04, Python 3.10+
# 실험: KoBERT 토큰 증분 문장 내 의미 분절 탐지
# ============================================================

# 딥러닝 프레임워크 (CPU 전용)
# GPU 사용 시 아래 주석 참고
torch==2.5.1

# HuggingFace - 모델 로딩 및 토크나이저
transformers==4.57.6

# 한국어 토크나이저 (klue/bert-base 필수 의존성)
sentencepiece==0.2.1

# 시각화
matplotlib==3.9.4

# 차원 축소 (PCA), 정규화 (StandardScaler)
scikit-learn==1.5.2

# 행렬 연산
numpy==1.26.4

# ============================================================
# 설치 방법
# ============================================================
#
# [CPU 환경]
#   pip install -r requirements.txt
#
# [GPU 환경 - CUDA 12.1]
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# [GPU 환경 - CUDA 11.8]
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# [한국어 폰트 - 시각화용 (Linux만 필요)]
#   sudo apt-get install fonts-nanum
#   fc-cache -fv
#
# [최초 실행 시 자동 다운로드]
#   klue/bert-base 모델 약 440MB → ~/.cache/huggingface/ 에 캐시됨
# ============================================================