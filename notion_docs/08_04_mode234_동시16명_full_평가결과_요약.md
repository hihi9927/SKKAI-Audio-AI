# mode2/3/4 동시 16명 full 평가 결과

## 📅 날짜
2026-08-04

## 🔧 작업 내용

LibriSpeech test-other 전체(90챕터 / 2939파일 / 오디오 5.34h)를 대상으로 커밋 정책 3종을 동시 접속 16명 조건에서 측정.

- 러너: `run_concurrent_chapters.py` (챕터 단위 큐 + 단일 프로세스 asyncio 워커 16개)
- 실행 스크립트: `paper_result/ASR/scripts/serve_mode{N}.sh` + `run_mode{N}.sh --clients 16`
- 공통 조건: chunk 200ms, send-interval 200ms, trailing silence 5.5s, 번역·GPT 교정 모두 off
- 결과 경로: `evaluation/LibriSpeech/paper_result/ASR/mode{2,3,4}/full/concurrent16_run01/`
- 세 모드 순차 실행, 총 소요 2시간 1분 (00:25 → 02:26). 모드당 39~42분

모드별 설정은 mode2가 always-commit(baseline 1.0.0), mode3이 dot-commit(baseline 1.0.0), mode4가 seg-commit(한국어 파인튜닝 `Doo12/Qwen3-ASR-1.7B-ko-silence-v4c900-merged`).

## 📊 결과 / 수치

### 핵심 지표

| 모드 | 정책 | WER | FSL avg | FTL | 커밋 수 | tok/커밋 | model_runtime |
|---|---|---|---|---|---|---|---|
| mode2 | always-commit | 10.98% | 0.0642s | 2.069s | 10,317 | 5.12 | 0.680s |
| mode3 | dot-commit | **5.74%** | 0.2876s | 7.915s | 3,975 | 13.39 | 2.596s |
| mode4 | seg-commit (ko finetuned) | 13.47% | 0.2680s | 4.764s | 5,828 | 9.65 | 0.962s |

### FSL 분포

| 모드 | p50 | p95 | p99 |
|---|---|---|---|
| mode2 | 0.0642s | 0.0847s | 0.0952s |
| mode3 | 0.1344s | 1.2077s | 1.6465s |
| mode4 | 0.0992s | 0.8058s | 3.0558s |

### 커밋 사유 비율

| 모드 | always | dot | seg | vad | finish |
|---|---|---|---|---|---|
| mode2 | 100.0% | 0.0% | 0.0% | 0.0% | 0.0% |
| mode3 | 0.0% | 99.2% | 0.0% | 0.0% | 0.8% |
| mode4 | 0.0% | 0.0% | 99.7% | 0.0% | 0.3% |

세 모드 모두 의도한 커밋 정책대로 동작했음을 확인.

### 동시성 지표

세 모드 공통으로 makespan 2307~2310s, 큐 효율 96%, 워커 유휴시간 합 약 1509s(최대 171s). 파일럿(sample 16챕터)의 큐 효율이 56%였던 것에 비해 크게 개선 — 90챕터가 워커 16개에 훨씬 고르게 분배되기 때문.

### 해석

**WER과 지연이 정확히 트레이드오프 관계.** mode3이 WER 5.74%로 mode2의 절반 수준이지만 FSL은 4.5배, FTL은 3.8배 느리다. 커밋당 토큰 수(5.12 vs 13.39)가 원인을 그대로 보여준다 — mode2는 2초 청크를 문장 경계와 무관하게 잘라 커밋하므로 단어가 토막나고, mode3은 문장이 확정될 때까지 기다려 문맥이 온전한 상태로 커밋된다.

**mode2의 지연 안정성이 두드러진다.** FSL p99가 0.0952s로 p50(0.0642s)과 거의 차이가 없다. 반면 mode3은 p99 1.65s, mode4는 p99 3.06s로 꼬리가 길다. 실시간성이 중요한 구간에서는 mode2의 예측 가능성이 강점.

**~~mode4의 WER 13.47%는 모델 불일치에 따른 예상 범위.~~** ← **이 해석은 틀렸다. 아래 "mode4 재측정" 절 참조.** 원인은 모델의 영어 성능이 아니라 반복 루프가 finish 커밋으로 새어나간 파이프라인 결함이었고, 같은 조건에서 영어 파인튜닝 모델로 재측정하니 WER 4.55%가 나왔다.

**makespan은 모드 간 변별력이 없다.** 러너가 send-interval 200ms로 실시간 페이싱을 하므로 makespan 상한이 GPU 속도가 아니라 오디오 길이와 워커 수로 결정된다. 이 벤치마크에서 비교해야 할 지표는 WER과 FSL이며, 처리량 비교가 목적이라면 페이싱 없는 별도 실행이 필요하다.

---

## 🔁 mode4 재측정 (영어 파인튜닝 모델) — 같은 날 추가 작업

### 왜 다시 쟀나

위 mode4 실행(WER 13.47%)을 뜯어보니 **2939파일 중 finish 커밋이 발생한 18개 파일(0.6%)이 WER 7.9%p를 혼자 만들고 있었다.** 이 18개를 빼면 WER 5.64%로, 나머지 2921파일에서는 오히려 세 모드 중 가장 깨끗했다.

| 모드 | 전체 WER | finish 파일 제외 WER | S | I | D |
|---|---|---|---|---|---|
| mode2 | 10.90% | 10.90% (finish 0건) | 6.36% | 2.77% | 1.77% |
| mode3 | 5.81% | 5.19% (2906파일) | 3.00% | 1.76% | 0.43% |
| mode4(ko) | 13.53% | 5.64% (2921파일) | 3.63% | **0.85%** | 1.16% |

*(자체 정규화로 재계산한 값이라 metric.json과 0.1%p 내외 차이가 있다. 상대 비교용.)*

### 원인 — 반복 루프가 dedup을 통과해 finish로 새어나감

서버 로그(`6938-70848-0015`, 참조 31단어 → 가설 556단어):

```
[SEG-IN-TEXT] text='Upon my return, I visited Smallney. <SEG> No such accusation was made. <SEG>'
[SEG-IN-TEXT] text='... <SEG> No such accusation was made. <SEG> No such accusation was made. <SEG>'
[COMMIT-SKIP] reason=rep-dedup   text='No such accusation was made.'
[COMMIT-SKIP] reason=cross-dedup text='No such accusation was made.'
   ... (반복)
```

1. ko 파인튜닝 모델이 영어 오디오에서 반복 루프에 빠진다 — `rep-dedup` 발동이 **mode4-ko 8,047회 vs mode3 27회, mode2 0회**
2. 서버는 반복을 정확히 감지해 커밋을 스킵한다 (`COMMIT-SKIP`). 여기까진 정상
3. 그런데 스킵된 텍스트는 `state.text`에 미커밋으로 남아 `committed_len`이 전진하지 않는다
4. trailing silence 5.5초가 계속 청크로 들어가고, 매 청크마다 `max_new_tokens=128`씩 루프 텍스트가 더 쌓인다 (`6938-70848-0015`: 오디오 11.84초인데 chunk_encode_log에 audio_pos 12.0/14.0/16.0 청크 존재, 545토큰 ≈ 4청크 × 128)
5. 스트림 종료 시 `finish_streaming()` → `flush_uncommitted(force=True)`가 **누적 버퍼 전체를 dedup 없이 그대로 커밋**

핵심은 `flush_uncommitted`에 반복 dedup이 없었다는 것 — 스트리밍 커밋 경로의 반복 방어가 finish 경로에서 완전히 우회된다. mode2가 무사한 이유는 always-commit이 매 청크를 즉시 커밋해 미커밋 버퍼가 쌓일 여지가 없기 때문(finish 0건).

### mode3의 finish 33건은 원인이 다르다

mode3은 반복이 아니라 **dot 확정 게이트가 성립하지 못한 경우**다. 모델이 미완성 가설의 프론티어에 항상 임시 마침표를 붙이는데:

```
chunk  8  '...sorrowing deeply for his loss.'                     → DOT-PENDING
chunk  9  '...for his lost friend, the Lord of Conings.'          → DOT-PENDING revised
chunk 10  '...the Lord of Coningsburgh, and he and his followers.' → DOT-PENDING revised
   ... (매 청크 수정되며 끝까지 확정 실패)
```

- 규칙1(context): 마침표가 문장 맨 끝이라 tail이 비어 롤백 창 밖 판정 불가
- 규칙2(stable): 문장이 매 청크 자라며 경계 키가 바뀜
- 규칙3(stall): 토큰 수가 계속 증가 중

데이터도 일치한다 — 33건 중 **24건이 내부 문장경계가 없는 긴 run-on 문장**, **13건은 nseg=1(파일 전체가 finish 한 방)**, 평균 길이 8.7s(전체 평균 6.5s), 31/33이 마침표로 끝남(마침표가 없어서가 아니라 확정을 못 해서). 비용은 작다(WER 5.81 → 5.19). 다만 발화 전체가 한 번에 나가므로 체감 지연이 최악이다.

### 재측정 결과 — mode4 (en-silence-c80, full 2939파일, 동시 16명)

`Doo12/Qwen3-ASR-1.7B-en-silence-c80-merged`로 동일 조건 재실행. 결과: `mode4/full/concurrent16_en_run01/`

| 지표 | mode4-ko (기존) | **mode4-en (재측정)** |
|---|---|---|
| WER | 13.47% | **4.55%** |
| S / I / D | 3.63 / 16.41 / 1.13 %* | **3.60 / 0.62 / 0.54 %** |
| finish 커밋 | 18건 (0.31%) | **13건 (0.21%)** |
| finish 세그 최대 토큰 | **545** | **5** |
| `rep-dedup` 발동 | 8,047회 | **0회** |
| 한글 혼입 파일 | 42 | **0** |
| 파일 WER > 1.0 | 13 | **0** |
| 커밋 수 / tok당 | 5,828 / 9.65 | 6,314 / 8.27 |
| FSL avg / p50 / p95 / p99 | 0.268 / 0.099 / 0.806 / 3.056s | 0.621 / 0.115 / 3.089 / 6.726s |
| FTL | 4.764s | 4.369s |
| makespan | 2307s | 2307s |

*ko의 S/I/D는 문제 챕터 서브셋(1439파일) 기준이라 전체 수치와 다르다.

남은 finish 13건은 성질이 완전히 다르다 — `'Then he says,'` `'And she said,'` `'He grumbled,'`처럼 **인용문 도입부 2~5토큰 꼬리**가 SEG를 못 받고 남은 정상적인 잔여 flush다. WER 영향은 무시할 수준.

**이 시점의 세 모드 비교 (full 2939파일, 동시 16명)** — mode3은 아래 "mode3 중복 커밋" 절에서 다시 갱신된다.

| 모드 | 정책 | 모델 | WER | FSL avg | FTL | tok/커밋 |
|---|---|---|---|---|---|---|
| mode2 | always-commit | baseline | 10.98% | 0.0642s | 2.069s | 5.12 |
| mode3 | dot-commit | baseline | 5.74% | 0.2876s | 7.915s | 13.39 |
| **mode4** | **seg-commit** | **en-silence-c80** | **4.55%** | 0.6212s | 4.369s | 8.27 |

다만 mode4의 FSL 꼬리(p95 3.09s, p99 6.73s)는 mode2/3보다 길다 — WER은 가장 좋지만 지연 최악값에서는 mode3에 밀린다.

### 코드 수정 — finish 경로 반복 방어

`Qwen3-ASR/examples/streaming_websocket_server.py`에 `_collapse_repetition()`을 추가하고 `flush_uncommitted`가 커밋 직전에 호출하도록 했다. en 모델에서는 루프가 안 생겨 증상이 없지만, 결함 자체는 모델과 무관하게 남아 있으므로 방어한다.

- **주기 n의 반복**을 찾는다. 연속-동일 문장만 접으면 폭주 9건 중 4건(`"A. B. A. B. …"` 형태의 2문장 사이클)이 안 잡힌다. 긴 주기부터 시도해 `"A B A B"`를 A·B 각각의 1주기 반복으로 오판하지 않게 했다.
- **`max_repeats=2`** — 실제 발화에도 같은 문장 2회 반복이 있다(`3005-163389-0001` 정답이 "TEAR DOWN THE FENCE TEAR DOWN THE FENCE"). 2회까지 보존하면 정상 반복은 안 건드리고 루프만 자른다.

기록된 커밋 텍스트에 오프라인 적용해 검증:

| 대상 | 결과 |
|---|---|
| ko 폭주 9건 접기 | **9/9** (545w→10w, 529w→157w, 322w→14w 등) |
| mode3 finish 33건 | 변경 0/33 |
| mode3 전체 커밋 | 변경 0/3975 |
| mode4-en 전체 커밋 | 변경 0/3125 |

---

## 🔁 mode3 중복 커밋 제거 — 같은 날 추가 작업

### 왜 팠나

mode4가 mode3를 이기는 게 이상했다. 문장이 확정될 때까지 기다리는 dot-commit이 문맥상 유리한데 WER은 밀렸다. S/I/D로 쪼개 보니 **인식 능력은 실제로 mode3이 더 좋았다**:

| | WER | 치환(S) | 삽입(I) | 삭제(D) |
|---|---|---|---|---|
| mode3 (dot, baseline) | 5.81% | **3.03%** | **2.34%** | 0.44% |
| mode4 (seg, en-silence-c80) | 4.76% | 3.60% | 0.62% | 0.54% |

치환률 3.03% vs 3.60%로 mode3 우위. 뒤집힌 건 삽입 하나 때문이었다.

### 원인 — 같은 문장이 철자만 바뀌어 여러 번 커밋됨

`7105-2340-0005` (참조 63단어 → 가설 143단어):

```
dot  "Alfred Fichencourt had covered himself with honors in the course of Marlborough's campai..."
dot  "Alfred Fychencote had covered himself with honors in the course of Marlborough's campaig..."
dot  "Alfred Pichenot  had covered himself with honors in the course of Marlborough's campaigns"
```

`7975-280057-0002`도 같은 패턴 (`Beresheba` → `Bertha` → `Brusheba`).

dot 확정 게이트가 문장을 커밋한 뒤 모델이 재디코딩하며 **고유명사 철자만 바꿔** 같은 문장을 다시 내놓는다. 기존 `cross-dedup`은 `_asr_key`(공백·대소문자·끝 구두점만 정규화한 뒤 **완전 일치**)로 판정하므로 철자가 달라지면 새 문장으로 보고 또 커밋한다. mode3 총 삽입 1,224단어 중 **911단어(74%)** 가 이 재방출이었다.

커밋 경계에서 단어 몇 개 겹치는 흔한 문제(36단어, 0.07%)는 두 모드 다 거의 없었다 — 문장 통째 재방출이 원인.

### 수정 — 두 군데였다

**v1. `_cross_dup_match()`** — 커밋 루프(`_process_slot_updates`)의 cross-dedup을 유사도 판정으로 보완.
- 6단어 미만은 완전 일치만 본다. `He said yes.` / `He said no.`처럼 실제로 다른 짧은 문장이 유사도만으로는 구분되지 않는다.
- 임계 0.85 (full 2939 튜닝값). 0.95면 철자 변형을 놓치고, 최소 단어수를 10으로 올리면 짧은 재방출을 놓친다.
- `always_commit`(mode2)은 커밋 단위가 문장이 아니라 청크라 제외 — 기존 `seg-boundary-dedup`과 같은 판단.

**v2. `_drop_already_committed()`** — `flush_uncommitted`가 커밋 직전 문장 단위로 같은 판정을 적용.

v1만으로는 절반밖에 안 잡혔다. `flush_uncommitted`가 `committed_asr_set`을 **아예 보지 않아서**, dot으로 커밋된 문장을 finish flush가 그대로 재방출하고 있었다:

```
3538-142836-0017:  dot    'In all the operations for preserve making, ...'
                   finish 'In all the operations for preserve making, ...'   ← 같은 문장
```

**finish 경로가 스트리밍 커밋 경로의 가드를 우회하는 패턴이 오늘만 세 번째다** — 반복 루프 누수(`_collapse_repetition`), 재방출 누수(`_drop_already_committed`), 그리고 mode3 finish 33건. `flush_uncommitted`가 커밋 루프의 방어를 하나도 공유하지 않는 구조적 문제.

### 100건 검증 (중복 발생 43건 + 무작위 대조 57건)

중복 파일을 일부러 모은 세트라 절대값은 코퍼스 WER과 비교 불가. 동일 세트 전후 비교만 유효하다.

| 집합 | before | v1 (커밋루프) | **v2 (+flush)** |
|---|---|---|---|
| 전체 100 | 47.31% | 22.83% | **8.03%** |
| 중복 43 | 77.19% | 35.69% | **10.60%** |
| 대조 57 | 4.34% | 4.34% | **4.34%** |

대조군 57건은 세 버전 모두 소수점 4자리까지 동일 — 회귀 0건. (동시 16명 ↔ 순차 실행 간에도 출력이 그대로 재현됐다. greedy decoding이라 동시성은 타이밍만 바꾸고 토큰 시퀀스는 안 바꾼다.)

### full 재측정 결과 — mode3 (baseline, 2939파일, 동시 16명)

결과: `mode3/full/concurrent16_dedupfix_run01/`

| 지표 | mode3 (기존) | **mode3 (dedup 수정)** |
|---|---|---|
| WER | 5.74% | **4.04%** |
| S / I / D | 3.03 / **2.34** / 0.44 % | 3.04 / **0.59** / 0.47 % |
| 커밋 수 | 3,975 (dot 3942 / finish 33) | 3,926 (dot 3867 / **finish 59**) |
| tok/커밋 | 13.39 | 13.32 |
| FSL avg / p50 / p95 / p99 | 0.2876 / 0.134 / 1.208 / 1.646s | 0.2804 / 0.132 / 1.191 / 1.645s |
| FTL | 7.915s | 7.918s |

- 치환률은 그대로(3.03 → 3.04%), **개선분 전부가 삽입**(2.34 → 0.59%)
- 기존 중복 43건: WER 77.19% → 12.51%
- 나머지 2,896건: 3.90% → 3.87% (회귀 없음)
- 악화된 파일 **11건 / 2939**, 총 추가 오류 28단어
- 지연 지표는 사실상 불변 — dedup은 커밋 여부만 바꾸고 타이밍은 안 건드린다

**최종 세 모드 비교 (full 2939파일, 동시 16명)**

| 모드 | 정책 | 모델 | WER | S | I | D | FSL avg | FSL p99 | FTL |
|---|---|---|---|---|---|---|---|---|---|
| mode2 | always-commit | baseline | 10.98% | 6.36% | 2.77% | 1.77% | 0.0642s | 0.095s | 2.069s |
| **mode3** | **dot-commit** | baseline | **4.04%** | 3.04% | 0.59% | 0.47% | 0.2804s | 1.645s | 7.918s |
| mode4 | seg-commit | en-silence-c80 | 4.55% | 3.60% | 0.62% | 0.54% | 0.6212s | 6.726s | 4.369s |

**정확도는 mode3, 지연 안정성은 mode2, 절충은 mode4.** mode3이 WER·FSL p99 둘 다 mode4보다 낫지만 FTL이 7.9s로 가장 느리다(문장 확정까지 기다리는 정책의 대가). mode4는 FTL 4.4s로 빠른 대신 FSL 꼬리가 p99 6.7s로 길다.

**단, mode3 vs mode4는 모델이 다르다** (baseline vs en-silence-c80). 위 표의 차이는 "정책"이 아니라 "정책 + 모델"의 합이며, 정책만 비교하려면 같은 가중치로 dot/seg를 각각 돌려야 한다. 미실시.

### 남은 결함 1건

`6432-63722-0035` (+15단어). finish flush가 이미 커밋된 문장을 **접두사로 포함한 더 긴 문장**을 내놨다. 길이 차이로 유사도가 0.85 아래로 떨어져 dedup을 통과했다.

```
dot    'Before the big wind in Ireland, suggested Thong with a nod at his Irish compatriot.'
finish 'Before the big wind in Ireland, suggested Thong, with a nod at his Irish compatriot,
        slightly laughed the Colonel.'
```

완전 중복이 아니라 **접두사 중복**이라 현재 판정으로는 안 잡힌다. 접두사 스트립을 넣으면 해결되지만 2939건 중 1건이라 이번엔 두었다.

## ⏭ 해결되지 않은 작업

- 결과 커밋 4개(`da94531` mode2, `fabce6f` mode3, `d77bef7` mode4, `dcb15e3` eval-run 수정) + 이번 재측정분이 로컬에만 있고 원격에 push되지 않음. 해당 머신에 git credential helper가 없어 자동 push가 실패했으며, 수동 push 필요.
- ~~mode4의 영어 WER은 모델 불일치 조건이라 모드 간 직접 비교에 쓸 수 없음.~~ → 해결. `concurrent16_en_run01`로 재측정 완료(WER 4.55%).
- **`_collapse_repetition()` 수정이 오프라인 검증만 됐다.** 기록된 ko 커밋 텍스트에 적용해 9/9 접힘·오검출 0건을 확인했지만, `flush_uncommitted` 배선을 실제로 태운 end-to-end 재현은 안 했다. en 모델로는 루프가 안 생겨 재현이 불가능하고, ko 모델 재실행은 하지 않기로 결정. 서버 정상 동작만 sample 벤치마크로 확인.
- **mode4의 FSL 꼬리가 왜 긴지 미규명.** p95 3.09s / p99 6.73s로 mode2(0.095s) · mode3(1.65s)보다 뚜렷하게 길다. seg-commit이 SEG 토큰을 기다리는 구조 때문인지, en-silence 파인튜닝의 SEG 방출 지연 때문인지 분리되지 않음. 07/30 FTL/FSL 분석([07_30 문서](07_30_FTL_FSL_mode3_4_비교분석_요약.md))의 "SEG 파인튜닝이 청크 경계에서 SEG를 주저한다"는 관찰과 이어질 가능성이 있음.
- mode3의 finish(dot 확정 게이트 미성립)는 원인만 규명하고 수정하지 않음. 긴 run-on 발화에서 발화 전체가 한 번에 커밋되는 문제라 체감 지연에 직접 영향. dedup 수정 후 오히려 33건 → 59건으로 늘었는데(커밋 루프에서 걸러진 문장이 미커밋으로 남아 finish로 넘어가는 것으로 보임) 확인 안 함.
- **정책만 비교하는 실행이 미실시.** 현재 mode3 vs mode4는 baseline vs en-silence-c80으로 모델이 달라, dot/seg 정책의 순수 효과가 분리되지 않는다. 같은 가중치로 dot·seg를 각각 돌려야 함. (`scratchpad/run2_dot_en.sh`에 en-silence-c80 + dot 실행 스크립트를 만들어 뒀으나 실행하지 않음.)
- **접두사 중복은 여전히 통과한다.** `6432-63722-0035` 1건. 완전 중복이 아니라 이미 커밋된 문장을 접두사로 포함한 더 긴 문장이라 유사도 판정을 빠져나간다. 접두사 스트립 추가로 해결 가능.
- **mode2/mode4는 dedup 수정 코드로 재측정하지 않았다.** 오프라인 예측상 영향은 미미하지만(mode2 1/10317, mode4-en 2/6314 커밋), 위 최종 비교표의 mode2·mode4 수치는 수정 전 코드 기준이다.
- 동시 접속 수를 바꿔가며(예: 1/4/8/16/32) 부하 곡선을 그리는 작업은 미실시. 현재는 16명 단일 지점만 측정됨.
