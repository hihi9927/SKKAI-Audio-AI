## 📅 날짜
2026-07-29

## 🔧 작업 내용

**1. 모드2 실험 격리**
- 논문용 실험(ASR/AST)을 기존 공유 인프라(서버 코드, 벤치마크 클라이언트)와 분리해서 관리하는 방향으로 정리. 서버/클라이언트 코드는 최대한 무수정, 대신 `evaluation/LibriSpeech/paper_result/` 아래에 launch 스크립트를 두는 구조로 결정.
- `paper_result/ASR/scripts/serve_mode2.sh`, `run_mode2.sh` 생성. 모드2(always-commit, baseline(1.0.0) 모델) 실험을 고정 플래그로 실행하고, 결과를 `paper_result/ASR/mode2/{scope}/{tag}/`에 저장하도록 구성.
- ASR 실험은 번역이 필요 없으므로 `--target-lang ""`으로 번역(Google Translate) 호출 자체를 스킵하도록 설정. GPT 번역/교정은 기본값이 off라 별도 처리 불필요.
- mode3, mode4, AST로 확장할 것을 고려해 스크립트를 `ASR/scripts/`로 한 번 더 정리(결과 폴더와 스크립트 폴더 분리).
- 실행 시점 자동 검증 도입: `streaming_websocket_server_fsl.py`의 `hello.serverConfig`에 `enable_dot_commit`, `always_commit` 필드 추가(기존 필드는 유지, 추가만). `paper_result/check_server_config.py`를 범용 검증기로 새로 작성 — `--expect key=value` 형태로 어떤 모드/데이터셋에서도 재사용 가능. `run_mode2.sh`에 벤치마크 실행 전 이 검증을 연결해서, 서버 설정이 기대값과 다르면 즉시 중단되도록 함.

**2. `/eval-run` 스킬 문서(`eval-run.md`) 섹션 2 정리**
- 섹션 2(원격 서버 선택)에서 AWS EC2 `describe-instances` 조회, EC2 기동/대기, `aws_app` 전용 systemd 서비스(`qwen-asr`) 중단 절차를 전부 제거하고, 고정 SSH alias `gpu` 사용으로 단순화.
- 수정 전 실제 서버 값 4가지를 직접 조사해서 문서에 반영: 저장소 경로 `/home/skkai/STiTy`, conda env(실제로는 `stity`), GPU 1장(RTX 4090), 부팅 시 자동 기동 systemd 서비스 없음.
- 섹션 4/5에 남아있던 오래된 conda 명령 오류 수정(5곳): `~/miniconda3` → `~/miniforge3`, `conda activate qwen3-asr` → `conda activate stity`.

## 🐛 발견된 문제 및 해결
- 모드2(always-commit) 여부는 기존 `hello.serverConfig`에 노출되지 않아 클라이언트가 원격 서버의 실제 설정을 검증할 방법이 없었음 → 서버 쪽에 필드 2개 추가해서 해결.
- `eval-run.md`가 예전 AWS EC2 인프라(aws_dev/aws_app/aws_test 인스턴스) 기준으로 작성돼 있었으나, 실제로는 온프레미스 4090 GPU 박스로 운영이 옮겨간 상태라 문서와 실제 구성이 어긋나 있었음 → 섹션 2를 실제 구성에 맞게 재작성.
- 문서상 conda env 이름(`qwen3-asr`)이 실제 서버 env 이름(`stity`)과 불일치 확인 → 섹션 4/5 명령어 전체 수정.
- `gpu` SSH alias가 실제로는 `~/.ssh/config`에 존재하지 않음을 확인. 처음엔 `skkai_4090_vpn`(13.209.199.242)을 GPU 서버로 추정했으나, 실제 점검 결과 GPU 없는 별도 EC2(VPN 중계용 추정)로 판명 — 진짜 GPU 서버는 이 세션이 SSH로 접속해 있는 로컬 박스 자체였음.
- loopback(`ssh gpu` → localhost) 자체인증을 시도했으나, 이 박스의 `authorized_keys`엔 팀원 노트북들의 공개키만 등록돼 있고 이 박스 자신의 공개키는 없어 `Permission denied (publickey,password)`로 실패. alias를 실제로 동작시키려면 config 추가와 공개키 등록(authorized_keys) 두 가지가 모두 필요하다는 점 확인.

## ⏭ 해결되지 않은 작업
- mode3, mode4용 `serve_modeN.sh` / `run_modeN.sh`는 아직 미작성 (기존 mode2 스크립트를 템플릿으로 재사용 예정).
- AST(번역 포함) 실험 스크립트/폴더 구성 아직 미착수.
- `gpu` SSH alias 최종 완성 여부 미결정 — config만 추가할지, authorized_keys에 자체 공개키까지 등록해서 완전히 동작하게 할지, 혹은 alias 없이 로컬 명령으로 대체하는 현재 상태를 유지할지 사용자 확인 대기 중.
