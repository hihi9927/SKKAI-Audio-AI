## 📅 날짜
2026-07-29

## 🔧 작업 내용

**1. mode2~4 VAD 비활성화**
- `serve_mode2.sh`, `serve_mode3.sh`, `serve_mode4.sh`에 `--no-vad` 추가. 기존엔 세 스크립트 모두 VAD 끄는 설정이 빠져 있어, always-commit/dot-commit/seg-commit 의도와 무관하게 VAD 트리거로도 커밋이 발생할 수 있는 상태였음.
- `streaming_websocket_server_fsl.py`의 `hello.serverConfig`에 `no_vad` 필드 추가 (기존엔 노출 안 돼서 클라이언트가 검증 불가능했음).
- `run_mode2/3/4.sh`의 `check_server_config.py` 사전 검증에 `--expect no_vad=true` 추가.

**2. mode2/3/4 동시 실행을 위한 포트 분리**
- 기존엔 serve/run 스크립트 모두 기본 포트(8765)를 그대로 써서 동시 실행 시 포트 충돌 발생.
- serve_mode2/3/4.sh와 run_mode2/3/4.sh(check_server_config.py, test_qwen3_librispeech.py 호출부)에 각각 `--port` 명시: mode2=8765, mode3=8766, mode4=8767.
