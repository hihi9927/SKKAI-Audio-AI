# autoseg 간소화 작업 노트

진행 중인 작업을 실시간으로 적는 문서. 브랜치 `autoseg-temp`.

**목표 5가지**
1. 불필요한 파라미터·수치 기준 제거
2. 결정론으로 되는 건 LLM 안 쓰기
3. 전체 구조 간소화 (중복 기능 제거)
4. 에이전트 루프에서 데이터 누출 용납 불가
5. LLM 입력은 구조화하여 전달

---

### 파라미터

| 이름 | 정의 | 의미 |
|---|---|---|
| **T** | 조각 당 목표 단어 (글자) 개수 | T 그리드에 따른 지연 변화 |
| **min_gap** | 조각 최소 크기 | 조각 크기의 하한 |
| **t_floor** | 라벨링 밀도 하한 | 최소 몇 단어 (글자)마다 분절이 존재해야하는지 |

띄어쓰기 하는 언어면 단어, 아니면 글자 기준으로 세 파라미터 정의

셋의 관계:

```
1200ms (파라미터)
   × 발화속도 (강제정렬 실측, 단위/초)
   ↓
min_gap  ──▶  t_floor = max(min_gap + 1, ceil(1.25 × min_gap))
                 └──▶  T 격자 = t_floor × {1, 1.5, 2, 3}      최종 곡선
                       루프 격자 = t_floor × {1, 1.5, 3}      비용 때문에 3점만
```

루프가 `t_floor` 를 안 보고 있었다 (`{1.5, 3}`). 근거 있는 조합이 아니라 옛 기본값
`--t-grid 3 6` 의 잔재였는데, 격자가 `min_gap` 유도로 위로 밀리면서 **아래 절반이 통째로**
빠졌다. 커버리지 요건은 `t_floor` 로 강제하면서 점수는 거기를 안 재고 있었다. 앞·중간·뒤
세 점으로 바꿨다 — 채택된 프롬프트가 한 번도 최적화 안 된 점에서 심판받는 걸 막는다.

**정하는 건 시간 하나뿐**이고 나머지는 다 따라 나온다.

`min_gap`이 하는 일은 "청자가 알아들을 수 있는 최소 방출량"이라 **문장 길이와
무관**해야 한다. 그래서 시간으로 정하고, 발화속도로 단위 환산만 한다.

| | 발화속도 | 토큰 (어절, 글자) 당 시간 | min_gap |
|---|---|---|---|
| de | 2.43 어절/초 | 0.41초 | 3 어절 |
| en | 2.88 어절/초 | 0.35초 | 3 어절 |
| zh | 4.74 자/초 | 0.21초 | 6 자 |
| ja | 5.77 자/초 | 0.17초 | 7 자 |

발화속도는 앞뒤 무음을 제거한 **발화 구간**으로 잰다. 파라미터 1200ms를 발화속도로
나눠 min_gap이 토큰 단위로 바뀌고, 그 값이 프롬프트에 들어간다.

`t_floor`는 하한 두 개 중 큰 쪽이다.

| | 값 | 어느 단계 | 어기면 |
|---|---|---|---|
| **포화 바닥** | `min_gap + 1` | 자를 때 | 곡선에 같은 점이 겹침 |
| **마킹 한계** | `1.25 × min_gap` | 표시할 때 | 성공할 수 없는 재시도 |

포화 바닥은 유도된다 — `T ≤ min_gap`이면 요청이 용량을 넘어 전부 같은 결과가 된다.
저장된 런 5개에서 5/5가 `min_gap + 1`이었다.

마킹 한계는 실측이다 — 실제 마킹 간격이 de 1.13배 · zh 1.20배 · ja 1.24배다.
물리적 상한은 `min_gap`(1.0배)인데 모델이 그 77~86%만 찍는다. 능력의 벽이 아니라
관측된 행동이라, 더 촘촘히 찍는 게 확인되면 내릴 수 있다.

어절 언어(min_gap 3~4)는 포화 바닥이, 글자 언어(6~8)는 마킹 한계가 결정한다.


### 조각 수 계산

```
조각 수  = max(1, round(문장길이 / T))         chunk_budget()
경계 수  = 조각 수 - 1                         boundaries()
용량     = max(0, 문장길이 // min_gap - 1)     capacity()

남길 경계    = 경계 수(T)                      truncate()
표시 요구량  = min(경계 수(t_floor), 용량)     coverage_need()
```

`boundaries()` 를 두 번 쓴다 — 넣는 T만 다르다.

| | 넣는 T | 나오는 값 | 언제 |
|---|---|---|---|
| **표시** | `t_floor` | 최소 몇 개 찍어라 (표시 요구량) | 분절 1회 |
| **절단** | 격자 `T` | 그중 몇 개 쓸까 | T마다 매번 |

---

### A0 — 데이터 준비 `data.py`
문장을 읽어서 train/dev/test로 나누고, 코퍼스 특성을 잰다.

```
1. 로더가 파일 읽음 → {id, text} 목록
2. 층화 정렬: 길이 3분위 × 구두점 유무로 묶고 라운드로빈으로 섞음
3. 앞에서부터 test → dev → train 순으로 떼감
   (train을 뒤에 두는 이유: train 크기를 바꿔도 test/dev가 안 밀림)
4. 코퍼스 특성 측정 (measure_profile)
   - 공백 비율 중앙값 > 0.02  →  띄어쓰기 하는 언어
   - 단위 결정: 띄어쓰기 언어면 어절, 아니면 글자
   - 중앙 길이 → min_gap 유도에 씀
   - 구두점 목록: 앞 텍스트에 90% 이상 붙어 나오는 문자만
```

### A1 — 언어 프로파일 `agents.py` · **LLM 1회**
샘플 20문장을 보여주고 언어 특성을 JSON으로 받아, 첫 프롬프트를 쓴다.

```
LLM이 채우는 것: 어순, 절 경계 신호, 함정 표현, 군말, 문체
측정으로 덮는 것: 띄어쓰기 여부, 구두점 목록  ← LLM 값은 무조건 짐
```

### A2+A3 — 분절과 검증 `pipeline.py` · **LLM + 결정론**

**둘은 한 함수다.** `segment_batch` 가 부르고, 그 안에서 고치고 검사하고 재시도까지 한다.

```
segment_batch(프롬프트, 문장들)
  ├ 캐시 확인            cached()   — 키 만드는 곳도, 빈 값 버리는 곳도 여기 하나
  ├ 배치 호출            6문장씩 묶어서 (사고 토큰 절약).  결과는 pre_map 에 미리 채움
  └ 문장마다 one()
      ├ pre_map 에 없으면 단건 호출
      ├ normalize_tags   표기 자동 수정
      ├ validate         규칙 검사
      └ 위반 남으면 LLM 재시도 1회 (단건으로만)
```

`n` 은 확신도 순위다 (1이 가장 확신). 위치 순서가 아니다.

> **분절·정규화 로직을 고치면 재개·재평가할 런의 `cache/segment.json` 을 지운다** — 캐시엔 정규화 *후* 문자열만 남아 옛 코드 산물이 조용히 섞인다 (새 런은 빈 캐시라 무관).


| | 하는 일 |
|---|---|
| `normalize_tags` | 번호 다시 매기기(**순위 관계 보존**), 맨앞/맨뒤 태그 삭제, 연속 태그를 같은 자리로 합치고 **가장 확신한 번호**를 남기기, 태그 뒤 구두점 옮기기, 좌우 공백 맞추기 |
| `validate` | 원문 훼손 · 태그 개수 부족 · 무번호 태그 — **셋뿐** |
| `_self_check` | 정규화가 자기 약속을 지켰나. 걸리면 프롬프트가 아니라 **정규화 버그** |

**연속 태그는 번호를 잃지 않는다.** `<SEG:1> <SEG:4>` 는 사이에 글자가 없으니 같은 경계다.
예전에는 뒤엣것을 남겨 4위로 살아남았고, 모델이 1등으로 꼽은 자리가 `truncate` 에서 먼저
잘렸다. 지금은 min(1,4)=1 로 대표한다. 단 **맨앞/맨뒤에서 떨어지는 태그의 번호는 안 물려준다**
— 그 자리는 애초에 무효라, 확신도를 유효한 자리에 이식하면 안 된다.

**구두점도 같은 빈 조각에 걸린다.** 태그 뒤 구두점은 앞 조각으로 되돌리는데, 바로 앞이
연속 태그 사이의 빈 조각이면 거기 얹혀 구두점 하나짜리 조각이 되살아났다
(`aa <SEG:2> <SEG:1> , bb` → `aa <SEG:2> , <SEG:1> bb`). 경계 하나를 쉼표에 쓰고,
태그를 떼도 원문과 달라져 `text_modified` — 유일한 채점 차단 위반 — 로 잡혔다.
지금은 **가장 가까운 내용 있는 조각**으로 보낸다.

**간격 위반은 정규화가 결정론으로 쳐낸다 — LLM 재시도로 안 넘긴다.** `gap_too_small` 은
v2 위반의 절반~전부였고(253건, de-en 78% · en-multi/run06 92%) **1차 통과율 0.5056** 의
주범이었다. 분절 호출의 절반이 재시도를 한 번 더 타고 있었다 — 비용의 90%가 분절이니 ≈1.5배.

원인은 프롬프트로 못 고치는 것이었다. 위반의 대부분이 **한 칸 모자란다**(min_gap=3 에서
71%가 2어절, =6 에서 39%가 5자, =8 에서 43%가 7자). 문면에 `_GAP_RULE` 이 이미 있는데도
모델이 의미적으로 좋아 보이는 자리가 가까우면 그냥 찍는다.

그런데 **어차피 `truncate` 가 안 쓸 태그다.** 너무 가까운 두 태그 중 순위 낮은 쪽을 지우면
되고, 그게 절단기가 나중에 하는 일과 같은 규칙이다. `normalize_tags(min_gap=)` 가 미리
쳐내니 검증기에 도달할 때 이미 간격이 지켜져 있다 — 마킹 시점에 규칙을 거는 원래 목적
(사후에만 걸면 절단이 상위 순위를 건너뛰어 평균 순위 1.92 → 2.98)이 그대로 달성되고
**LLM 호출만 사라진다.** 랜덤 20,000건 검증에서 절단기 필터가 **100% 항등**이었다.

**검증기에서 표기 규칙 6종을 뺐다.** 맨앞/맨뒤 태그·연속 태그·태그 좌우 공백·번호 중복·번호
결번은 정규화가 먼저 고쳐서 **구조적으로 못 뜬다** (마지막 실측이 ko-en/run01, 정규화 도입
직전). 죽은 규칙을 위반 목록에 두면 산출물이 "검사하고 있다"고 거짓말한다. 대신 같은 항목을
`_self_check` 가 **버그 신고**로 본다 — 프롬프트 잘못과 정규화 잘못은 대응이 완전히 다르다.
무번호 태그(`bad_priority_format`)만 남겼다: 전부 무번호면 정규화가 그대로 두기 때문이다(비교군).

**재시도 프롬프트가 규칙을 다시 쓰지 않는다.** `system=prompt` 로 프롬프트를 통째로 다시 주니
`[Output Rules]` 가 이미 있다. 예전엔 번호 규약과 커버리지 지침을 재시도 문자열에 복사해 뒀는데,
A1 이 규칙을 고치면 1차 호출과 재시도가 **서로 다른 규칙**을 요구하게 된다. 덤으로 비교군 사고도
막는다 — 옛 문구는 무번호 `<SEG>` 를 쓰는 사람 프롬프트에게 "1..N 을 매겨라"고 시키고 있었다.

**부분 순위는 지웠다.** 상위 N개만 번호 요구해 사고량 아끼려던 배선인데,
정렬 생략이 평가 생략이 아니라 사고가 오히려 +17% 늘었다 (`docs/RANK_METRIC_DIAGNOSIS.md`
§8.3). 본 루프에서 한 번도 켜진 적 없음.

### A4 — 절단 `pipeline.py` · **결정론**
노브 T에 맞춰 경계를 골라낸다. 여기가 지연을 정하는 곳.

```
조각 수 = max(1, round_half_up(문장길이 / T))
남길 경계 = 조각수 - 1 개

고르는 법: 순위 1등부터 차례로,
          단 이미 고른 자리·문장 양끝과 min_gap 이상 떨어져야 함
못 채우면 덜 자른다 (억지로 보충 안 함)
```

**보장되는 것**: 조각 개수, 평균 조각 크기(산수), 최소 조각 크기(min_gap)
**보장 안 되는 것**: 최대 조각 크기, 고른 간격

**반올림은 올림 하나로 통일했다.** 파이썬 `round()` 는 짝수 반올림이라 `round(2.5)==2` 다.
조각 수는 그걸 쓰고 T 격자 유도는 `int(x+0.5)` 를 써서, 같은 파라미터 사슬에 규칙이 둘이었다
— min_gap=3 격자에서 (길이, T) 조합의 7.1%가 갈렸다 (T=4·10어절 → 2조각 vs 3조각).
저장된 결과 기준 (문장, T) 쌍의 **6.6%에서 조각 수가 바뀐다** — 옛 점수와 직접 비교 불가.

**마킹과 절단이 같은 자 하나를 쓴다.** 태그 위치 계산(`tag_positions`)이 예전엔 `validate` 의
간격 검사와 `truncate` 에 같은 6줄로 복붙돼 있었다. 둘을 일치시키는 게 설계의 요점이라
특히 나쁜 중복이었다.

### A5 — 번역 `pipeline.py`
두 종류를 뽑는다.

```
전체 번역  — 문장 통째로. 정답 대용(오라클)으로 씀. 캐시 영구
조각 번역  — 조각 하나씩, 앞 조각들만 보고 번역. 뒤는 절대 안 봄
```

**번역기는 Google 하나다.** LLM 번역기 분기는 지웠다 — v2 런 26개가 전부 Google 이었고,
목적함수가 "운영에 옮겨 붙는가"를 재는 것이라 운영이 쓰는 번역기여야 한다.

**Google 접근 경로가 둘이다. 같은 회사인데 번역문이 다르다.**

| | 정체 | 인증 |
|---|---|---|
| `gtx` | 웹 번역 위젯이 쓰는 **비공식** 엔드포인트 | 없음 |
| `v2` | 공식 Cloud Translation **Basic** (API 버전 2) | API 키 |

`GOOGLE_TRANSLATE_API_KEY` 가 있으면 v2, 없으면 gtx. `--translate-backend` 로 고정한다.

gtx 는 인증이 없어 Google 이 구분할 수 있는 게 IP 뿐이라, 이 실험이 6일간 30만 건을
호출해 **IP 단위로 429 를 맞았다**(2026-08-26 현재도 막혀 있다). 429 면 HTML 차단
페이지가 와서 JSON 파싱이 터지고 번역칸만 조용히 빈다. 프로덕션 서버도 같은 이유로
v2 로 옮겼다.

**두 백엔드는 같은 자가 아니다 — 기존 gtx 캐시 18건을 v2 로 재번역해 일치 0/18.**

```
gtx: It's this month, but they say there's an engineering college M-T in March.
v2 : It's February, but I heard the engineering department has an MT in March.
       └ 2월을 "this month" 로 잘못 옮긴 게 gtx
```

그래서 캐시 키와 `translator_id` 에 백엔드가 들어간다. 섞이지는 않지만,
**gtx 로 잰 기존 26개 런의 점수는 v2 런과 비교할 수 없다.**

### 쓰는 모델

고를 수 있게 열어 두면 런마다 다른 모델로 잰 값이 한 표에 섞인다. 실제로 섞여 있었다
(NLI 저장분: xlmr-anli 19 / mdeberta-xnli 16 / deberta-mnli 7). 각 자리에 **하나씩만** 둔다.

| 자리 | 모델 | 왜 이것 |
|---|---|---|
| 분절·에이전트 | `gpt-5-mini` | `gpt-5.4-mini` 대비 비용 1/3.9 에 품질 차 검출 안 됨 |
| 번역 | Google Translate (gtx) | 운영 서버와 같은 경로. 결정론적이라 번역기 잡음 0 |
| adequacy | CometKiwi (`wmt22-cometkiwi-da`) | 참조 없는 QE. **y축 주지표** |
| contradiction | `xlm-roberta-large-xnli-anli` | 조기 방출 검출. 다국어 large 여야 함 |
| consistency | 같은 NLI (양방향 함의) | 보고용. 모델을 contradiction 과 공유해 GPU 1벌 |

**NLI 는 다국어 large 가 아니면 안 된다.** base 급 `mDeBERTa-v3-base-xnli` 는 ko/zh/ja
타깃에서 consistency 곡선이 뒤집혔다 — T 를 키울수록 통째 번역에서 멀어진다고 나오는데,
경계를 빼기만 하므로 물리적으로 불가능한 방향이다. 같은 데이터에서 comet·chrf 는 5/5 정상.
영어 전용 `deberta-large-mnli` 는 분리가 가장 깨끗하지만 비영어 타깃에서 무음으로 틀린다.

**embed·chrf consistency 백엔드는 삭제했다** — 45개 런에서 사용 0회(nli 39 / comet 4).
embed 하나 때문에 채점 모듈이 임베딩 API(`Gateway`)를 물고 있었다.

### A6 — 채점 `metrics.py` · **결정론**
```
adequacy       조각별 번역 품질(참조 없음). 조각 길이로 가중 평균
contradiction  경계마다: (전체번역) vs (그때까지 누적 방출)이 모순인가.
               경계들의 평균. 경계가 없으면 미정의
effective      adequacy × (1 - contradiction)      ← 목적함수
laal_words     지연. 조각 크기로 가중.  **집계에서만 해석** (아래)
consistency    합본 vs 전체번역 양방향 함의의 min   ← 보고용
```

**contradiction 의 잡음 바닥은 방향이 뒤집혔다.** NLI 는 무해한 미완성에도 모순 확률을 준다.
그 크기가 "짧을수록 크다"고 문서·주석 곳곳에 적혀 있었는데, 그건 삭제된 `deberta-mnli`
성질이다. 현행 `xlmr-anli` 는 **길수록 크다** — German 0.024 → 0.107 (재측정
`runs/noise_floor_xlmr/`, 문장 611).

**신호는 모든 길이에 있다.** 같은 문장·같은 길이에서 (우리 방출) vs (오라클 prefix) 를
짝지어 3,000개 경계를 재면 초과분이 +0.011~+0.038 로 **평평하다.** 바닥이 차지하는 비율은
길이와 함께 오르지만(23%→80%) 목적함수에 들어가는 건 절대값이다.

빼지는 않는다 — 바닥은 *아무 데나 자른* prefix 로 재고 우리 경계는 *고른 자리*다. 차이의
중앙값이 ~0 이고 우리가 더 나쁜 경계는 22~31% 뿐이라, 빼면 대부분 0 으로 눌린다.
평균을 만드는 건 그 꼬리고, 지표의 일이 바로 그 꼬리를 잡는 것이다.

**지연(`laal`)은 문장 단위로 단조가 아니다.** 조각을 줄이면 지연이 반드시 오른다고
적어 뒀는데, 문장 단위에서는 11.7%가 반대로 간다. LAAL 이 "소스를 다 들은 시점" 이후를
안 세기 때문이다(표준 정의) — 조각을 합치면 그 시점이 앞당겨져, 가장 오래 기다린 항이
집계에서 통째로 빠진다. **런 평균은 17/17 에서 단조 증가**라 곡선 자체는 안전하다.
구조적으로 보장되는 건 조각 수(`k`)가 준다는 것까지다.

**평균은 꼬리에 끌린다 — `p10` 을 같이 본다.** `effective` 평균이 소수의 나쁜 문장에 크게
움직인다. 실측(100문장): 평균 0.7760 / 중앙 0.8021 이고 **최악 1문장을 빼면 평균이 +0.0034**
오른다. 프롬프트 간 실제 차이가 0.003~0.007 이라 **같은 크기**다. 그래서 이터레이션 로그와
최종 곡선 표에 `eff p10`(하위 10% 지점)과 `eff min`(최악 문장)을 나란히 찍는다.

```
평균 ↑ 이고 p10 도 ↑    꼬리까지 좋아졌다 — 진짜 개선
평균 ↑ 인데 p10 은 ↓    그 배치에 나쁜 문장이 덜 걸렸을 수 있다
```

꼬리를 잘라내지는 않는다(절사평균 등). 이 지표가 잡으려는 게 바로 그 꼬리 —
조기 방출한 나쁜 경계 — 이기 때문이다.

**빈 값은 `None` 이지 0 이 아니다.** 무분절 문장은 모순을 낼 경계가 없어 *미정의*다 —
0(무죄)으로 넣으면 무분절이 자동 만점을 받는다. 목적함수는 None 을 평균에서 뺀다.
다언어 병합 경로만 `0.0` 을 쓰고 있었고(비교군이 그 모드에서만 0점으로 끌려 내려갔다),
이번에 통일했다.

### 확인해야 할 것 — `contradiction` vs `1 − entailment`

조기 방출을 **모순 확률**로 재고 있는데, **함의 확률**로 재는 쪽이 더 나을 수 있다.
오프라인 실측은 전부 그쪽을 가리키는데 표본이 얇아 아직 안 바꿨다.

| | 결과 |
|---|---|
| 관문 최소 여유 | 0.0604 → **0.8377** (14배). contra 가 `premature_scope`·`premature_head` 를 0.10·0.33 으로 겨우 잡는다 |
| 긴 방출 구간 신호 | +0.0115 → **+0.0450** (10-14어절). contra 가 약한 바로 그 구간 |
| 검출력 \|t\| | 0.49 → **0.80** (1.6배). 11회 중 7회에서 우세 |
| 두 척도 순위상관 | +0.923 — 축이 바뀌는 게 아니라 같은 걸 또렷하게 잰다 |
| 채택 결정 | 11회 중 **3회 뒤집힘**(27%). 1회는 부호까지 다름 |

**대가**: `1 − entail` 은 `neutral` 을 벌점에 넣는다. 미완성 조각은 원래 neutral 이 정상이라
설계가 일부러 피한 것이다. 관문에서 `benign_incomplete` 가 0.025~0.041 → 0.086~0.151 로
3~4배 올랐다 (premature 0.99 와는 여전히 6배 이상 벌어져 분리는 유지).

**판단 방법 — 구현됨.** NLI 한 번 호출에 세 라벨 확률이 다 나오므로 **추가 비용 0** 이다
(`ContradictionBackend.score_dual`). 교체하지 않고 `effective_ent` 를 나란히 기록한다 —
이터레이션 로그에 `[ent 0.7xxx]`, 최종 곡선 표에 `eff (ent)` / `contra (ent)` 열.
**목적함수·채택 판정에는 안 들어간다.** 다음 런의 로그로 판단한다. `se` 가 1.44배 커지므로
`--adopt-se-mult` 재조정이 함께 필요하다 (전례: mdeberta→xlmr-anli 때 1.0 → 0.5).

### A7 — 판정자 `agents.py` · **LLM**
**점수를 안 낸다. 사례에 설명만 붙인다.**

```
종전   문장 8개를 뽑아 그 안의 모든 경계를 판정 → premature_rate 라는 비율 생산
       그 비율이 Critic 지표 블록·최종 리포트로 감

지금   contradiction 상위 경계 N개만 판정 → cause / shift / generalized_rule 만
       그 사례에 붙어서 Critic 에게 감. 비율은 만들지 않는다
```

**왜 탐지기에서 뺐나.** 판정자의 `premature` 정의를 그대로 옮기면

> the emitted text asserts a proposition that the rest of the sentence **CONTRADICTS**

인데, 이건 `contradiction` 지표가 재는 것과 **글자 그대로 같은 질문**이다 (전제 = 문장 전체
번역, 가설 = 누적 방출). 그런데 저장된 판정에서 둘이 안 맞는다:

```
판정자 vs contradiction   AUC 0.663      ← 0.5 가 동전 던지기
mistranslated 로 찍힌 경계  contra 중앙 0.0699
safe 로 찍힌 경계           contra 중앙 0.0699    ← 같다
```

같은 질문에 답하는 두 측정이 서로 안 맞으면, **싼 쪽(NLI, 추가 호출 0)을 두고 비싼
쪽(LLM)을 탐지에 쓸 이유가 없다.** 게다가 그 판정이 `select_cases` 의 우선 쿼터를 채우고
있었으므로, Critic 이 보는 사례가 *점수를 실제로 깎는 문장*이 아니라 *판정자가 깎일 거라고
생각한 문장*으로 채워지고 있었다.

**비율도 편향돼 있었다.** 표본을 실패 조준으로 뽑고 그 표본으로 비율을 쟀다 — 조건부 상향
추정치다 (run03 test 0.2727). 그래서 리포트용으로 무작위 표본을 한 번 더 돌리는 우회로가
붙어 있었는데, 그 수를 결정에 쓰는 곳은 없었다. 그 test 판정 패스도 같이 지웠다.

**그래도 남긴 이유.** `contradiction` 은 쌍체 평균으로 뭉개져서 **어느 문장의 어느 자리가
왜 나빴는지가 안 남는다.** Critic 이 규칙을 지어내려면 그 재료가 필요하고, 그건 숫자가 아니라
말이다:

```
cause              polarity not yet settled | wrong participant | modifier scope |
                   head not yet arrived | referent lost
shift              경계를 오른쪽으로 몇 단위, 어떤 말 뒤로 옮겼어야 했나
generalized_rule   이 부류를 막을 규칙 한 줄
```

즉 **조준은 측정이, 설명은 LLM 이** 한다.

**Critic 에게 표본의 성격도 알려준다.** 판정 대상이 무작위가 아니라 *가장 나쁜 경계*라는
사실을 모르면 "4개 중 4개가 premature" 를 비율로 읽는다. 지시문에 *"여기서는 아무것도
세지 마라 — 왜인지만 읽어라"* 를 넣었다. `safe` 판정도 정보다 — **측정은 높게 나왔는데
사람이 보기엔 뒤집힌 게 없다**는 뜻이라, 그 경계엔 규칙을 쓰지 말라는 신호다.

호출도 준다. 문장당 모든 경계(≈3개)를 재던 것이 상위 경계 하나씩이 된다.
`--judge-rows` → `--judge-boundaries` 로 단위가 바뀌었다.

### A8 — 진단 `agents.py` · **LLM + 결정론**
실패 사례를 모아 말로 정리한다. **무엇을 고칠지는 Critic 이 정한다 — `focus` 는 없앴다.**

```
Critic 이 받는 것
   실패 사례 (contradiction 상위 + adequacy 하위 + 포맷 위반)
   그중 모순 상위 경계에는 판정자의 cause / shift 가 붙어 있다   ← A7
   지표 전체 + 각 지표의 뜻 + [프롬프트로 움직일 수 있는가]   ← metrics.GLOSSARY
   순위 감사 (어떤 표면 특징을 과신하는가)
   고착 힌트 (직전에 고쳐서 실패한 섹션)
```

**지표는 뜻과 함께 넘긴다 — 그리고 무엇이 프롬프트의 몫인지 표시한다.**
Critic 에게 가는 지표가 31개인데 **설명이 있는 건 7개뿐**이었다. 목적함수 `effective` 조차
정의된 적이 없다 — 무슨 숫자를 올려야 하는지 모르는 채로 진단하고 있었다.

설명을 `metrics.GLOSSARY` 로 코드 옆에 두고 프롬프트가 거기서 **생성**한다. 손으로 쓰면
필드가 늘어도 안 따라오는데(방금 넣은 `effective_ent` 도 설명이 없었다), 생성하면 빠진 게
`(no description)` 으로 드러난다.

각 항목에 **프롬프트로 움직일 수 있는가**를 붙인다. 못 고치는 걸 고치라고 시키는 게 루프가
개선을 못 만든 큰 원인이었다:

```
움직일 수 있음   score · effective · adequacy · contradiction ·
                 rank_lift · missing_boundaries
못 움직임        laal_words (T가 정한다) · chunks_per_sentence (절단기가 고정한다) ·
                 format_pass_rate (남는 건 원문 훼손) · 그 외 진단·보고 지표
```

특히 `chunks_per_sentence` 설명에 **"덜 자르라고 시켜도 이 값은 안 준다 — 어느 경계가
살아남는지만 바뀐다"** 를 명시했다. 개정의 79%가 금지 추가였는데 그게 조각 수를 못 줄인다는
사실(실측 18런 평균 +0.051)을 PE 가 모르고 있었다.

**`rank_lift` 는 Critic 에게 설명과 함께 넘긴다.** 순위를 무작위로 섞었을 때 `effective` 가
얼마나 떨어지는지 — 경계 **위치**와 분리해 **순위**만 재는 값이다.

```
rank_lift 크다   순위가 실제로 일하고 있다 → [Priority Rules] 를 더 다듬을 가치 있음
rank_lift ≈ 0    순위에 정보가 없다 → [Priority Rules] 를 고쳐봐야 소용없다.
                 문제는 "어디를 찍느냐" 지 "어떻게 줄 세우냐" 가 아니다
```

지금까지 **어느 런에도 값이 기록된 적이 없다** — 기능이 마지막 런보다 나중에 들어왔다.
그래서 `focus="priority"` 판정도, Critic 이 순위축을 읽는 것도 근거가 비어 있었다.
`m.to_dict()` 에 실려 가긴 했지만 **무엇인지 알려주지 않아** LLM 이 읽지 못했다.
다언어 병합에서 값이 통째로 떨어지던 버그도 함께 고쳤다.


**왜 `focus` 를 없앴나.** 지표에서 방향을 결정론으로 뽑아 PE 에게 "측정에서 나온 것이니
따르라"(Rule 7)고 강제했는데, 네 갈래 어느 것도 자기 목표를 못 고쳤다 — 목표 지표 개선
**33~36%**, 동전 던지기보다 못하다.

| 갈래 | 무너진 이유 |
|---|---|
| `format` (15회) | 위반의 65%가 간격이었고 이제 정규화가 처리. 12회 시도해서 못 고침 (재시도 후 +0.0021, 1차 −0.0257) |
| `priority` (4회) | `rank_lift` 가 전 런에서 null — 판정 기준이 비어 있는데도 발동 |
| `coverage` (6회) | `missing_boundaries` 가 전 런 ~0 — 마찬가지 |
| `placement` (18회) | "그 외" 기본값. 최다인데 contra·eff 를 둘 다 악화 |

게다가 섹션 제한은 `focus="priority"` 일 때만 실제로 강제됐다(4/43회). 나머지는 계산만 하고
안 썼으니 조종은 사실상 PE 프롬프트 Rule 7 하나였다 — **근거가 빈 판정을 "측정값"이라는
권위로 최우선 규칙으로** 만든 셈이다. 상수 3개(`MISSING_BOUNDARIES_LIMIT`,
`PREMATURE_LIMIT`, `RANK_LIFT_T_MIN`)도 함께 사라졌다.

**고착 방지는 남기되 핸들을 바꿨다.** `focus` 라벨 → **직전에 고친 섹션**. 실측상 연속
이터레이션이 같은 섹션을 다시 고친 게 8/30(27%)인데, 라벨이 달라도 같은 섹션을 고치는
경우가 많아 focus 로는 못 잡았다.

**PE 규칙도 바꿨다.** "focus 를 따르라" → "움직일 수 없는 지표에 개정을 쓰지 마라" +
**"금지를 추가하기 전에 무엇이 대신 잘릴지 생각하라"**. 개정의 79%가 금지 추가였는데,
절단기가 조각 수를 고정하므로 금지는 조각 수를 못 줄이고 **어느 경계가 살아남는지만** 바꾼다.


### A9 — 프롬프트 수정 `agents.py` · **LLM**
프롬프트를 고친다. 통과해야 하는 관문 3개.

```
골격     필수 섹션이 다 있나                        (check_skeleton)
구조     섹션을 새로 만들거나 없애지 않았나          (check_revision)
타깃무관 한 문장 안에 타깃 언어명 + 문법 근거가 있나 (check_target_agnostic)
```

셋 다 통과한 뒤 **분량 예산**을 본다. 넘치면 거부가 아니라 압축이다 (아래 "국소성 관문을
없앴다" 참조).

**타깃무관 게이트를 실측으로 검증했다.** 저장된 프롬프트 190개에 세 규칙을 걸어 봤다:

```
언어명만          발동 68%   대부분 [Role] 의 "will be translated into English",
                            zh 소스의 "or English text"(라틴 문자 설명). 못 쓴다
문법어만 (종전)   발동 33%   43건 중 5건 오검출 — ko-en/run01 전체가
                            "Korean is head-final and verb-final" 로 걸렸다
연언 (지금)       발동  8%   16건 전부 en→de 의 진짜 타깃 문법 규칙. 오검출 0
```

버그 두 개가 나왔다. **(1)** 문법어에 소스 면제가 없어서 소스 문법을 설명한 문장이
걸렸다 (위 5건). **(2)** `_TARGET_LANG_WORDS` 에 `english` 가 빠져 있었는데 지금 모든
런(de/ja/zh/ko→en)의 타깃이 English 라 **그 런들에서는 언어명 검사가 구조적으로 아무것도
못 잡았다.**

연언으로 바꾼 이유는 **정밀도가 재현율보다 중요**해서다 — 오검출은 이터레이션을 통째로
날리지만(관문 기아 = 영구 교착), 미검출은 그 규칙이 5개 타깃 평균에서 값을 못 하면 채택
판정이 거른다. 이 게이트는 싼 사전확률이고 진짜 방어선은 다중 타깃 목적함수다.

**PE 에게 알려준 목적함수가 v1 것이었다.** 지시문 끝에 이렇게 적혀 있었다:

```
score = average adequacy across latency budgets
The only hard condition is 100% valid output format.
```

둘 다 틀렸다. 실제는 `mean over T of (adequacy × (1 − contradiction))` 이고, 포맷 하드
게이트는 v1 에서 폐기됐다. 즉 **PE 는 목적함수의 주된 항(contradiction)을 모른 채** 개정하면서
**존재하지 않는 조건(포맷)을 최우선으로** 좇고 있었다. 문장별 산포가 contradiction 쪽이
adequacy 의 **3.2배**(0.175 vs 0.054)라 실제로 점수를 끄는 건 그쪽이다.

**결정론이 이미 처리하는 것도 몰랐다.** 번호 재배치·맨앞뒤 태그·연속 태그·구두점·간격은
정규화가 다 고친다. 그걸 프롬프트로 시키는 규칙은 이터레이션 낭비다. 무엇이 자동 수리
범위이고 무엇이 점수까지 살아남는지를 지시문에 명시했다.

**"금지 79%" 는 PE 의 일탈이 아니라 설계된 동작이었다.** 지시문에 *"위험한 경계는
`[Never Segment]` 가 아니라 `[Priority Rules]` 아래쪽에 두라"* 고 적혀 있다. PE 는 그대로
따랐고 그래서 `[Priority Rules]` 를 22회 고쳤다. 문제는 그 demote 가 효과가 있느냐인데,
그걸 재는 `rank_lift` 가 여태 비어 있었다 — 다음 런에서 처음 보인다.

**Critic 제안을 전부 반영한다 — 종전에는 하나씩 나눠 실었다.** 신용 배분(어느 규칙이
도움됐나)을 얻으려던 것인데 성립하지 않는다:

```
한 규칙짜리 개정의 |Δ| 중앙   0.00505
dev 225 검출 한계             0.0104        ← 두 배 차이
dev  60 검출 한계             0.0220        ← 네 배 차이
```

**전체가 도움됐는지조차 못 재는 상태에서 규칙별 신용 배분은 환상이다.**

"큰 개정은 쌍체 검정력을 파괴한다"는 근거도 실측이 반박한다 — 저장된 판정 14회에서
변경 문장 비율 ~ `|t|` 순위상관 **+0.165**, ~ `|Δ|` **+0.376** 이다. 많이 바뀔수록 효과가
크고 검출력은 비슷하다. 그리고 **역대 최대 성과가 97% 변경 개정**이었다 (Δ +0.0296, 채택).
같은 구간에 역대 최악(t −3.17)도 있으니 큰 개정은 크게 좋거나 크게 나쁘다 — 가리는 것이
채택 판정의 일이고, dev 3.7배 + 선택 편향 제거로 이제 가릴 수 있다.

후보 K개는 유지하되 **규칙 부분집합이 아니라 표현 차이**로 나뉜다.

**국소성 관문을 없앴다 — 남은 제한은 분량 하나다.** 종전 관문은 두 가지를 봤다:
변경 섹션 ≤ N개, 그리고 변경 섹션의 길이 ≤ M배. 둘 다 *"한 군데만 고쳐라"* 를 강제하는데,
Critic 제안을 전부 반영하기로 한 지금 필요한 개정은 반대 모양이다 — **여러 섹션을 한 줄씩
고치는 편집**이 섹션 개수에 걸리고, 짧은 섹션에 두 줄 넣는 편집이 비율에 걸린다.

실측 로그 26개에서 관문이 실제로 막은 것:

```
범위 위반 6건 중   성장 5건 · 섹션 개수 1건 · 섹션 추가/삭제 0건
```

**막고 있던 것은 사실상 "분량" 하나뿐**이었으므로 분량 하나만 남긴다. 그리고 분량은
거부할 일이 아니라 줄일 일이므로 넘치면 압축기로 보낸다.

```
구조 게이트   섹션 추가/삭제 금지     (check_revision — 이게 전부다)
분량 예산     v0 × 1.3                넘치면 압축 → 재검사 → 그래도 넘치면 거부
```

**분량 숫자는 어디서 왔나.** 처음엔 걸음 크기(직전 best 대비)와 런 천장(v0 대비) 둘을
뒀는데, 실측이 걸음 크기 쪽을 지웠다:

```
적용된 개정 42건        이터 간 배수  중앙 1.03 · p90 1.12 · 최대 1.29
                        v0 대비 배수  중앙 1.06 · p90 1.17 · 최대 1.29
v0 천장(1.3배) 발동                   42건 중 1건 (2%)
```

**PE 는 프롬프트를 크게 안 키운다.** 종전 상수 1.5 는 분포 밖이라 한 번도 발동한 적이
없는 죽은 파라미터였다. 그리고 종전 게이트가 "성장" 으로 죽인 개정 6건도 **전체로는
1.08~1.14배**밖에 안 됐다 — 짧은 섹션 하나가 2배가 됐을 뿐이다. 전체로 재면 전부 통과할
개정이었다.

그래서 숫자는 하나만 남는다: `--max-prompt-growth 1.3`. **품질 노브가 아니라 비용
천장**이다 — 프롬프트는 문장마다 다시 보내므로 길이가 곧 토큰 비용이고, 이터레이션을
거듭하며 사례집으로 부푸는 것을 막는 자리다. 2% 발동은 "PE 의 자연스러운 산출을 왜곡하지
않으면서 누적 팽창만 막는" 위치라는 뜻이다.

**예산을 지시문에도 글자 수로 적는다.** 종전에는 배수(1.25/2.5/4.0)로 말했는데 PE 산출이
1.39/1.36/1.79 로 **지시에 비례하지 않았다** — 배수는 PE 가 셀 수 없는 값이다. 자기 출력
길이는 셀 수 있으므로 `at most N characters` 로 준다. 지시문에는 "섹션은 몇 개든 고쳐도
되고, 캡은 합계 길이" 라고 명시하고, 넘치면 압축기가 깎는데 **압축기는 어느 문장이 새
아이디어였는지 모른다** 는 것까지 알려 준다 — 스스로 지킬 이유를 준다.

**신뢰 영역(적응형 반경)도 함께 없앴다.** 채택이면 ×1.2, 아니면 ÷1.5 로 움직였는데
실측 채택률이 3/37(8%) 이라 **한 방향으로만 도는 톱니**였다. 로그 전체에서 반경 변경
10줄 중 넓힘은 2회뿐이고, 첫 기각에 2.50 → 1.25 로 바닥을 친 뒤 올라온 적이 없다. 게다가
좁히는 사건의 대부분은 "나빴다" 가 아니라 **"잡음 안이라 모른다"** 였다 (판정 14회 중
|t|>2 는 1회). 모르는 것을 근거로 탐색 반경을 줄이고 있었다.

### A10 — 압축 `agents.py` · **LLM**
```
compress()   프롬프트가 분량 예산을 넘으면 줄인다.
             `sections_changed` 를 보호 목록으로 받아 **이번에 넣은 변경은 남기고**
             나머지(중복 예시 → 약한 예시 → 같은 말 하는 조건 → 군말) 순으로 깎는다
```

**경로가 하나다.** 종전에는 `compress`(런 전체 천장, v0 대비)와 `fit_sections`(국소성
성장 한도, 섹션별)가 따로 있었다. 재는 대상이 똑같이 "분량" 이므로 두 상한 중 작은 쪽을
한 숫자로 합치고 압축기도 하나만 남겼다. 호출도 후보마다가 아니라 **선택이 끝난 뒤 한 번**
이다 (K번 → 1번).

압축해도 예산을 못 맞추거나 골격이 깨지면 그때 거부한다.

### A11 — 루프 제어 `loop.py` · **결정론**
```
dev 실행:  train 쌍체 Δ > −1×se     확실히 나쁜 것만 컷.  가르는 일은 dev 가 한다
채택:      dev 쌍체 Δ > 0.5×se
중단:      개선 없이 N번 / 최대 반복 / 예산 소진
```

**게이트를 낮춘 이유.** 종전 `train Δ > 0` 은 부호만 봐서 좋은 개정의 **3분의 1을 버렸다**
(train se 0.011~0.016 인데 실제 차이가 0.003~0.007). v2 실측: 개정 37회 중 게이트 차단 23회
(62%), 채택은 3회(8%)뿐이고 런 18개 중 13개가 v0 이후 아무것도 채택 못 했다. train 을 3배
키워도 유실이 37% → 28% 로밖에 안 준다 — 표본으로 못 고친다. 게이트가 아끼는 돈은 런당 ~$3.

**후보 선별은 train 에서 한다 — dev 는 채택 판정 전용.** 종전에는 `dev[:40]` 로 상위 2개를
추리고 그 둘을 **dev 전체**로 재채점해 골랐는데, 바로 그 dev 로 채택 판정을 했다. 고르는 데
쓴 자로 그 선택이 옳았는지 판정한 것이다. 후보 2개 중 최대값은 잡음 σ 일 때 `0.564σ` 만큼
위로 뜨고, dev 60 에서 σ≈0.011 이라 편향이 **≈0.006** — 재려는 효과(0.005)와 같은 크기다.
기준선과 새 개정본이 양쪽 다 부풀어 있어 쌍체 Δ 의 기대값이 정의되지 않았다.

2단계(probe→top2→dev)도 1단계(train)로 합쳤다. 2단계는 "dev 가 비싸니 아껴 쓰자"는 절충이라
dev 를 안 쓰면 필요 없다. **채점 비용은 같다** — `3후보×probe40 + 2후보×dev60 = 240문장`
→ `3후보×train80 = 240문장`. 선별 정확도는 문장 수만 따르므로(20문장 36% / 40문장 58% /
60문장 76%) 같은 예산을 한 단계에 몰면 오히려 **오른다**.

**분할은 균형을 안 맞춘다.** 여기서 train 은 프롬프트를 '학습'하지 않는다 — 맞출 파라미터가
없다. 하는 일은 ① 싼 게이트 ② Critic 이 볼 실패 사례 공급이고, 실질 신호는 전부 dev 다.
그래서 dev 에 몰아준다 (`train 40 / dev 265 / test 100`).

---